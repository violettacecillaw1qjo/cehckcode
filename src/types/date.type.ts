export type DateRangeValue = {
    from: number;
    to: number;
};

export type DateValue = number;

export type TimeValue = {
    hour: number;
    minute: number;
    second: number;
    millisecond: number;
};
